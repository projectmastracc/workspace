# Superseded

Use `protection-stack-template.md` for multi-compound / mitigate / neuroprotection queries.

Legacy interaction outline ideas are folded into that template (pathway inventory + per-side counters + assembled stack).
